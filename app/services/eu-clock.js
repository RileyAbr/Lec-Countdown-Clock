//Clock functionality and interval counter provided by ember-cli-clock
//https://github.com/jerel/ember-cli-clock
import Clock from 'ember-cli-clock/services/clock';

export default Clock.extend({
  interval: 1000
});
